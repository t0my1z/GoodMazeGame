#pragma once

//A enum used by a few classes to have a concept of direction. Used to know the direction of the neighbours of rooms, for example
enum class EDirection
{
	Up,
	Down,
	Left,
	Right,
	Count
};